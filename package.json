{
  "name": "@ssthouse/vue-tree-chart-options",
  "private": true,
  "author": "ssthouse",
  "scripts": {
    "install:deps": "lerna  bootstrap",
    "build:component": "lerna run build:component --scope @ssthouse/vue-tree-chart-options",
    "build:demo": "lerna run build --scope @ssthouse/vue-tree-chart-options",
    "release:all": "lerna run release"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
